<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gahyun.dev.mapper.ReservationsMapper">
  <insert id="insertResRooms" parameterType="ReservationsDto">
     INSERT INTO reservations
     	(user_id, room_id, check_in_date, check_out_date, total_price)
     VALUES
     	(#{user_id}, #{roomid}, #{startDate}, #{endDate}, #{totalPrice})
  </insert>
</mapper>
