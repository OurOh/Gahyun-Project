<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gahyun.dev.mapper.PaymentMapper">
    <!-- 결제 정보 삽입 -->
    <insert id="insertPayment" parameterType="com.gahyun.dev.model.PaymentsDto">
        INSERT INTO payments (reservation_id, payment_method, payment_amount, payment_date, payment_status)
        VALUES (#{reservation_id}, #{payment_method}, #{payment_amount}, NOW(), #{payment_status})
    </insert>
</mapper>